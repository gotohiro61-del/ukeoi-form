# CLAUDE.md — ukeoi-form（BOOKS請負報告フォーム）運用ルール（AI長時間運用対応）

作成日：2026-07-30
目的：Claude Codeが本リポジトリで安全に長時間自律作業するためのルールを定義する（エバースグループ共通ルール準拠）。

---

## 1. リポジトリの目的

- 「BOOKS 請負報告」— 請負大会の稼働報告を送る**スマホ向け1ファイル完結フォーム**
- 報告者名・日付・案件名・最寄り駅・スタッフ別の出退勤/休憩時間を入力し、GASウェブアプリ経由でスプレッドシートへ送信。送信履歴の閲覧機能つき
- **リポジトリ唯一のファイルは `index.html`（約570行）**。CSS／画面5つ（top・report・confirm・done・history）／JSが1ファイルに同居
- バックエンドのGASコードは本リポジトリに存在しない（別管理）
- **デフォルトブランチは `master`**（main ではない）

## 2. 技術構成

- HTML + CSS + Vanilla JavaScript（フレームワーク・ビルド・パッケージ管理・テストすべてなし）
- iOS風UIは手書きCSS。**UIのスタイルは既存の houkoku-form と揃える方針**（過去コミットで明示）。独自リデザインは方針違反

## 3. プロジェクト固有の地雷（必読）

1. **`index.html` 内の `API_URL`（250行付近）が本番GASバックエンドとの唯一の結線**であり、過去7コミット中4件がこの行の差し替え・CORS修正。**最も事故が多い箇所**。値の変更は、ユーザーがGASを再デプロイして得た新URLを提示したときのみ
2. **POST の `Content-Type: text/plain` はGASのCORSプリフライト回避のための意図的な指定**。`application/json` に「正す」と壊れる
3. fetchの `redirect` オプション追加も過去に障害を起こした経緯がある。復活させない
4. **`submitReport()` は本番スプレッドシートへの実書き込み**。動作確認目的でフォーム送信を実行しない
5. 573行が1ファイルに同居。部分編集時はCSS/HTML/JSのどのブロックかを厳密に特定する。**フォーマッタ一括適用禁止**（差分が全体に及びレビュー不能になる）

## 4. 機密情報・個人情報

- GASデプロイURLは公開前提の設計（認証なし）。この構造上の弱点は認識しつつ、独断で認証を追加しない（提案は可）
- 実在スタッフ名・実報告データをテスト入力に使わない
- `.gitignore` が存在しない。**今後ファイルを追加する場合は先に `.gitignore` 整備を提案する**（変更は要指示）

## 5. Git運用ルール（エバース共通）

- **master（デフォルト）ブランチ上で自動作業しない**。長時間のAI作業は専用ブランチ（ai-automation-setup 等）で行う
- 自動作業中のmasterへの直接コミット・push・mergeは禁止
- **force push禁止・rebase禁止**（履歴の書き換え禁止）
- `git reset --hard`・`git clean -fd` 等の破壊的Git操作は禁止
- `git add .`・`git add -A` は禁止。**コミット対象ファイルは個別に指定する**
- 作業開始前と終了時に `git status` を確認する
- 未コミット変更がある場合、既存変更と今回の変更を混ぜない
- **pushは明示的な指示がある場合のみ実行する**（自動push禁止）
- 別セッション・別PCからの変更が疑われる場合は、勝手にpull・merge・rebaseせず停止する

## 6. 長時間自動実行時のルール（エバース共通）

- 初回は**最大30分・最大3サイクル**まで（実績を確認してから段階的に拡大）
- 同じエラー・同じ修正を3回繰り返したら停止する
- 成功条件を実行前に明文化する（機械的に判定できる形で）
- 毎サイクル終了時に状態を保存し、途中再開可能にする
- 対象外ファイルへの変更を検知したら停止する
- 本番GASエンドポイントへの送信は行わない
- 自動コミットは専用ブランチ上で明示的に許可された場合のみ。**自動pushは行わない**

## 7. 品質チェック（作業後に確認）

- [ ] `API_URL`・`Content-Type: text/plain`・fetchオプションを変えていない（指示があった場合を除く）
- [ ] ブラウザで5画面（top・report・confirm・done・history）の表示を確認した
- [ ] houkoku-formとUIスタイルが乖離していない
- [ ] フォーマッタによる全体差分が発生していない
- [ ] `git diff` に意図しない変更がない

## 8. 停止条件

次の場合は作業を停止し、ファイルを変更せず報告する：
masterブランチ上にいる（自動作業時）／API_URLの変更が必要になった（新URL未提示）／本番送信が必要になった／破壊的Git操作が必要／同一エラーが3回続いた／指定時間・サイクル数に達した

## 9. 作業完了時の報告（エバース共通）

1. 実施した作業 2. 作成・変更したファイル 3. 変更理由 4. 実行した確認 5. 検出したリスク 6. 未解決事項 7. git status 8. コミットの有無 9. pushの有無 10. 次に推奨する行動
